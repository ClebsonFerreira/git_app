import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:git_app/app/modules/repos/repos_controller.dart';
import 'package:git_app/app/modules/repos/repos_module.dart';

void main() {
  initModule(ReposModule());
  // ReposController repos;
  //
  setUp(() {
    //     repos = ReposModule.to.get<ReposController>();
  });

  group('ReposController Test', () {
    //   test("First Test", () {
    //     expect(repos, isInstanceOf<ReposController>());
    //   });

    //   test("Set Value", () {
    //     expect(repos.value, equals(0));
    //     repos.increment();
    //     expect(repos.value, equals(1));
    //   });
  });
}

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:git_app/app/modules/repos/repos_page.dart';

void main() {
  testWidgets('ReposPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReposPage(title: 'Repos')));
    //  final titleFinder = find.text('Repos');
    //  expect(titleFinder, findsOneWidget);
  });
}
